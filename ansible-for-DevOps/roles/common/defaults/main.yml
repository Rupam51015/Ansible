---
# Kept to packages that exist by the same name on Ubuntu, RHEL 9, and
# Amazon Linux 2023 with no extra repos (htop needs EPEL on RHEL; installing
# curl conflicts with curl-minimal on Amazon Linux 2023).
common_packages:
  - vim
  - tree
  - wget
common_timezone: UTC
